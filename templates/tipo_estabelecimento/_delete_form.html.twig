<form method="post" action="{{ path('tipo_estabelecimento_delete', {'idtipoEstabelecimento': tipo_estabelecimento.idtipoEstabelecimento}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_estabelecimento.idtipoEstabelecimento) }}">
    <button class="btn">Delete</button>
</form>
