<form method="post" action="{{ path('item_estoque_delete', {'iditemEstoque': item_estoque.iditemEstoque}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_estoque.iditemEstoque) }}">
    <button class="btn">Delete</button>
</form>
