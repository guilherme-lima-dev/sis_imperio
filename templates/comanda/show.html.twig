{% extends 'base.html.twig' %}

{% block title %}Comanda{% endblock %}

{% block body %}
    <h1>Comanda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcomanda</th>
                <td>{{ comanda.idcomanda }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comanda_index') }}">back to list</a>

    <a href="{{ path('comanda_edit', {'idcomanda': comanda.idcomanda}) }}">edit</a>

    {{ include('comanda/_delete_form.html.twig') }}
{% endblock %}
