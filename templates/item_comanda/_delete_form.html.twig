<form method="post" action="{{ path('item_comanda_delete', {'iditemComanda': item_comanda.iditemComanda}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item_comanda.iditemComanda) }}">
    <button class="btn">Delete</button>
</form>
