{% extends 'base.html.twig' %}

{% block title %}ItemComanda{% endblock %}

{% block body %}
    <h1>ItemComanda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IditemComanda</th>
                <td>{{ item_comanda.iditemComanda }}</td>
            </tr>
            <tr>
                <th>NomeItem</th>
                <td>{{ item_comanda.nomeItem }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('item_comanda_index') }}">back to list</a>

    <a href="{{ path('item_comanda_edit', {'iditemComanda': item_comanda.iditemComanda}) }}">edit</a>

    {{ include('item_comanda/_delete_form.html.twig') }}
{% endblock %}
