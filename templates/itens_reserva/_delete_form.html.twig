<form method="post" action="{{ path('itens_reserva_delete', {'iditensReserva': itens_reserva.iditensReserva}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ itens_reserva.iditensReserva) }}">
    <button class="btn">Delete</button>
</form>
